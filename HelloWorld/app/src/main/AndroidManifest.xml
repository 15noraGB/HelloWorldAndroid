<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloWorld">

        <activity
            android:name=".AnadirProductoActivity"
            android:exported="false" />
        <activity
            android:name=".DetalleListaActivity"
            android:exported="false" />
        <activity
            android:name=".ListaCompraActivity"
            android:exported="false" />
        <activity
            android:name=".ResultadoHelloWorldActivity"
            android:exported="false" />
        <activity
            android:name=".HelloWorldActivity"
            android:exported="false" /> <!-- Esta es la Activity que se abre al iniciar la app -->
        <activity
            android:name=".NavegacionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Esta Activity solo se abre desde el botón -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>